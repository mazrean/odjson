// Package plain holds the types behind bench/shapes with no generated code
// attached: the reflection baseline. Package gen declares the same types with
// odjson's codecs; a test in bench/shapes checks that the two files differ
// only in their package clause.
package plain
