// Code generated by odjson. DO NOT EDIT.
//
// Source: odjson -direct

package direct

import (
	"bytes"
	"encoding/json/jsontext"
	"github.com/mazrean/odjson/odjsonrt"
	"slices"
)

// odjsonAppend appends the JSON encoding of v to dst.
func (v *Root) odjsonAppend(dst []byte, m odjsonrt.StringMode) ([]byte, error) {
	var err error
	_ = err
	start := len(dst)
	dst = append(dst, ",\"name\":\""...)
	dst, err = odjsonrt.AppendStringBodyChecked(dst, string(v.Name), m)
	if err != nil {
		return nil, err
	}
	dst = append(dst, "\""...)
	if m.V2() || v.Count != 0 {
		dst = append(dst, ",\"count\":"...)
		dst = odjsonrt.AppendInt(dst, int64(v.Count))
	}
	if v.Tags == nil {
		if m.V2() {
			dst = append(dst, ",\"tags\":[],\"rati"...)
			dst = append(dst, "o\":"...)
		} else {
			dst = append(dst, ",\"tags\":null,\"ra"...)
			dst = append(dst, "tio\":"...)
		}
	} else {
		dst = append(dst, ",\"tags\":["...)
		for i1 := range v.Tags {
			if i1 > 0 {
				dst = append(dst, ',')
			}
			dst = append(dst, "\""...)
			dst, err = odjsonrt.AppendStringBodyChecked(dst, string(v.Tags[i1]), m)
			if err != nil {
				return nil, err
			}
			dst = append(dst, "\""...)
		}
		dst = append(dst, "],\"ratio\":"...)
	}
	dst, err = odjsonrt.AppendFloat(dst, float64(v.Ratio), 64)
	if err != nil {
		return nil, err
	}
	dst = append(dst, ",\"nested\":"...)
	dst, err = v.Nested.odjsonAppend(dst, m)
	if err != nil {
		return nil, err
	}
	dst[start] = '{'
	if v.Ptr == nil {
		dst = append(dst, ",\"ptr\":null}"...)
	} else {
		dst = append(dst, ",\"ptr\":"...)
		dst, err = (*v.Ptr).odjsonAppend(dst, m)
		if err != nil {
			return nil, err
		}
		dst = append(dst, "}"...)
	}
	return dst, nil
}

// odjsonCapRootTags remembers how long v.Tags has been, to allocate it once.
var odjsonCapRootTags odjsonrt.CapHint

// odjsonParse decodes the JSON object starting at p into v and
// returns the offset just past it. Strings are interned through
// sc, which may be nil.
func (v *Root) odjsonParse(data []byte, p int, sc *odjsonrt.StringCache) (int, error) {
	var err error
	_ = err
	p = odjsonrt.SkipSpace(data, p)
	if np, ok := odjsonrt.ParseNull(data, p); ok {
		return np, nil
	}
	if uint(p) >= uint(len(data)) || data[p] != '{' {
		return p, odjsonrt.ErrType(data, p, "Root")
	}
	p++
	p = odjsonrt.SkipSpace(data, p)
	if uint(p) < uint(len(data)) && data[p] == '}' {
		return p + 1, nil
	}
	for {
		var key []byte
		if uint(p) >= uint(len(data)) || data[p] <= ' ' {
			p = odjsonrt.SkipSpace(data, odjsonrt.SkipIndent(data, p))
		}
		idx := -1
		rest := data[p:]
		if len(rest) > 3 {
			switch rest[3] {
			case 'm':
				if len(rest) >= 6 && string(rest[:6]) == "\"name\"" {
					idx, p = 0, p+6
				}
			case 'u':
				if len(rest) >= 7 && string(rest[:7]) == "\"count\"" {
					idx, p = 1, p+7
				}
			case 'g':
				if len(rest) >= 6 && string(rest[:6]) == "\"tags\"" {
					idx, p = 2, p+6
				}
			case 't':
				if len(rest) >= 7 && string(rest[:7]) == "\"ratio\"" {
					idx, p = 3, p+7
				}
			case 's':
				if len(rest) >= 8 && string(rest[:8]) == "\"nested\"" {
					idx, p = 4, p+8
				}
			case 'r':
				if len(rest) >= 5 && string(rest[:5]) == "\"ptr\"" {
					idx, p = 5, p+5
				}
			}
		}
		if idx >= 0 {
			if np := odjsonrt.AfterName(data, p); np > 0 {
				p = np
			} else if p, err = odjsonrt.AfterKey(data, p); err != nil {
				return p, err
			}
		} else {
			key, _, p, err = odjsonrt.ParseKey(data, p)
			if err != nil {
				return p, err
			}
			switch string(key) {
			case "name":
				idx = 0
			case "count":
				idx = 1
			case "tags":
				idx = 2
			case "ratio":
				idx = 3
			case "nested":
				idx = 4
			case "ptr":
				idx = 5
			}
		}
		switch idx {
		case 0:
			if np2, ok3 := odjsonrt.ParseNull(data, p); ok3 {
				p = np2
			} else {
				var x4 string
				x4, p, err = odjsonrt.ParseStringCached(data, p, sc)
				if err != nil {
					return p, err
				}
				v.Name = x4
			}
		case 1:
			if np5, ok6 := odjsonrt.ParseNull(data, p); ok6 {
				p = np5
			} else {
				if x7, np8, ok9 := odjsonrt.ParseDecimal(data, p); ok9 {
					v.Count = int(x7)
					p = np8
				} else {
					var x10 int64
					x10, p, err = odjsonrt.ParseInt(data, p, 64)
					if err != nil {
						return p, err
					}
					v.Count = int(x10)
				}
			}
		case 2:
			if np11, ok12 := odjsonrt.ParseNull(data, p); ok12 {
				p = np11
				v.Tags = nil
			} else {
				if uint(p) >= uint(len(data)) || data[p] != '[' {
					return p, odjsonrt.ErrType(data, p, "[]string")
				}
				p++
				s13 := v.Tags[:0]
				p = odjsonrt.SkipSpace(data, p)
				if uint(p) < uint(len(data)) && data[p] == ']' {
					p++
				} else {
					if cap(s13) == 0 {
						s13 = make([]string, 0, odjsonrt.CapFor[string](&odjsonCapRootTags))
					}
					for {
						var e14 string
						if uint(p) >= uint(len(data)) || data[p] <= ' ' {
							p = odjsonrt.SkipSpace(data, odjsonrt.SkipIndent(data, p))
						}
						if np15, ok16 := odjsonrt.ParseNull(data, p); ok16 {
							p = np15
						} else {
							var x17 string
							x17, p, err = odjsonrt.ParseStringCached(data, p, sc)
							if err != nil {
								return p, err
							}
							e14 = x17
						}
						s13 = append(s13, e14)
						p = odjsonrt.SkipSpace(data, p)
						if uint(p) >= uint(len(data)) {
							return p, odjsonrt.ErrSyntax(data, p, "unexpected end of JSON input")
						}
						if data[p] == ',' {
							p++
							continue
						}
						if data[p] == ']' {
							p++
							break
						}
						return p, odjsonrt.ErrSyntax(data, p, "after array element")
					}
					odjsonCapRootTags.Record(len(s13))
				}
				if s13 == nil {
					s13 = []string{}
				}
				v.Tags = s13
			}
		case 3:
			if np18, ok19 := odjsonrt.ParseNull(data, p); ok19 {
				p = np18
			} else {
				if x20, np21, ok22 := odjsonrt.ParseSimpleFloat(data, p, 64); ok22 {
					v.Ratio = float64(x20)
					p = np21
				} else {
					var x23 float64
					x23, p, err = odjsonrt.ParseFloat(data, p, 64)
					if err != nil {
						return p, err
					}
					v.Ratio = x23
				}
			}
		case 4:
			p, err = v.Nested.odjsonParse(data, p, sc)
			if err != nil {
				return p, err
			}
		case 5:
			if np24, ok25 := odjsonrt.ParseNull(data, p); ok25 {
				v.Ptr = nil
				p = np24
			} else {
				if v.Ptr == nil {
					v.Ptr = new(nested)
				}
				p, err = (*v.Ptr).odjsonParse(data, p, sc)
				if err != nil {
					return p, err
				}
			}
		default:
			p = odjsonrt.SkipSpace(data, p)
			p, err = odjsonrt.SkipValue(data, p)
			if err != nil {
				return p, err
			}
		}
		p = odjsonrt.SkipSpace(data, p)
		if uint(p) >= uint(len(data)) {
			return p, odjsonrt.ErrSyntax(data, p, "unexpected end of JSON input")
		}
		switch data[p] {
		case ',':
			p++
		case '}':
			return p + 1, nil
		default:
			return p, odjsonrt.ErrSyntax(data, p, "after object key:value pair")
		}
	}
}

// odjsonParseV2 is odjsonParse under encoding/json/v2's semantics, for
// input a jsontext.Decoder has already validated when strict is false,
// and for bytes nobody has looked at when it is true.
func (v *Root) odjsonParseV2(data []byte, p int, sc *odjsonrt.StringCache, strict bool) (int, error) {
	var err error
	_ = err
	p = odjsonrt.SkipSpace(data, p)
	if np, ok := odjsonrt.ParseNull(data, p); ok {
		*v = Root{}
		return np, nil
	}
	if uint(p) >= uint(len(data)) || data[p] != '{' {
		return p, odjsonrt.ErrType(data, p, "Root")
	}
	p++
	p = odjsonrt.SkipSpace(data, p)
	if uint(p) < uint(len(data)) && data[p] == '}' {
		return p + 1, nil
	}
	var seen [1]uint64
	_ = seen
	unknown, umark := odjsonrt.UnknownNames(sc)
	for {
		var key []byte
		if uint(p) >= uint(len(data)) || data[p] <= ' ' {
			p = odjsonrt.SkipSpace(data, odjsonrt.SkipIndent(data, p))
		}
		kp := p
		idx := -1
		rest := data[p:]
		if len(rest) > 3 {
			switch rest[3] {
			case 'm':
				if len(rest) >= 6 && string(rest[:6]) == "\"name\"" {
					idx, p = 0, p+6
				}
			case 'u':
				if len(rest) >= 7 && string(rest[:7]) == "\"count\"" {
					idx, p = 1, p+7
				}
			case 'g':
				if len(rest) >= 6 && string(rest[:6]) == "\"tags\"" {
					idx, p = 2, p+6
				}
			case 't':
				if len(rest) >= 7 && string(rest[:7]) == "\"ratio\"" {
					idx, p = 3, p+7
				}
			case 's':
				if len(rest) >= 8 && string(rest[:8]) == "\"nested\"" {
					idx, p = 4, p+8
				}
			case 'r':
				if len(rest) >= 5 && string(rest[:5]) == "\"ptr\"" {
					idx, p = 5, p+5
				}
			}
		}
		if idx >= 0 {
			if np := odjsonrt.AfterName(data, p); np > 0 {
				p = np
			} else if p, err = odjsonrt.AfterKey(data, p); err != nil {
				return p, err
			}
		} else {
			key, p, err = odjsonrt.ParseKeyV2(data, p, strict)
			if err != nil {
				return p, err
			}
			switch string(key) {
			case "name":
				idx = 0
			case "count":
				idx = 1
			case "tags":
				idx = 2
			case "ratio":
				idx = 3
			case "nested":
				idx = 4
			case "ptr":
				idx = 5
			}
		}
		switch idx {
		case 0:
			if strict && seen[0]&(1<<0) != 0 {
				return kp, odjsonrt.ErrDuplicateNameAt(data, kp)
			}
			seen[0] |= 1 << 0
			if np26, ok27 := odjsonrt.ParseNull(data, p); ok27 {
				p = np26
				v.Name = ""
			} else {
				var x28 string
				x28, p, err = odjsonrt.ParseStringV2(data, p, sc, strict)
				if err != nil {
					return p, err
				}
				v.Name = x28
			}
		case 1:
			if strict && seen[0]&(1<<1) != 0 {
				return kp, odjsonrt.ErrDuplicateNameAt(data, kp)
			}
			seen[0] |= 1 << 1
			if np29, ok30 := odjsonrt.ParseNull(data, p); ok30 {
				p = np29
				v.Count = 0
			} else {
				if x31, np32, ok33 := odjsonrt.ParseDecimal(data, p); ok33 {
					v.Count = int(x31)
					p = np32
				} else {
					var x34 int64
					x34, p, err = odjsonrt.ParseInt(data, p, 64)
					if err != nil {
						return p, err
					}
					v.Count = int(x34)
				}
			}
		case 2:
			if strict && seen[0]&(1<<2) != 0 {
				return kp, odjsonrt.ErrDuplicateNameAt(data, kp)
			}
			seen[0] |= 1 << 2
			if np35, ok36 := odjsonrt.ParseNull(data, p); ok36 {
				p = np35
				v.Tags = nil
			} else {
				if uint(p) >= uint(len(data)) || data[p] != '[' {
					return p, odjsonrt.ErrType(data, p, "[]string")
				}
				p++
				s37 := v.Tags[:0]
				p = odjsonrt.SkipSpace(data, p)
				if uint(p) < uint(len(data)) && data[p] == ']' {
					p++
				} else {
					if cap(s37) == 0 {
						s37 = make([]string, 0, odjsonrt.CapFor[string](&odjsonCapRootTags))
					}
					for {
						var e38 string
						if uint(p) >= uint(len(data)) || data[p] <= ' ' {
							p = odjsonrt.SkipSpace(data, odjsonrt.SkipIndent(data, p))
						}
						if np39, ok40 := odjsonrt.ParseNull(data, p); ok40 {
							p = np39
							e38 = ""
						} else {
							var x41 string
							x41, p, err = odjsonrt.ParseStringV2(data, p, sc, strict)
							if err != nil {
								return p, err
							}
							e38 = x41
						}
						s37 = append(s37, e38)
						p = odjsonrt.SkipSpace(data, p)
						if uint(p) >= uint(len(data)) {
							return p, odjsonrt.ErrSyntax(data, p, "unexpected end of JSON input")
						}
						if data[p] == ',' {
							p++
							continue
						}
						if data[p] == ']' {
							p++
							break
						}
						return p, odjsonrt.ErrSyntax(data, p, "after array element")
					}
					odjsonCapRootTags.Record(len(s37))
				}
				if s37 == nil {
					s37 = []string{}
				}
				v.Tags = s37
			}
		case 3:
			if strict && seen[0]&(1<<3) != 0 {
				return kp, odjsonrt.ErrDuplicateNameAt(data, kp)
			}
			seen[0] |= 1 << 3
			if np42, ok43 := odjsonrt.ParseNull(data, p); ok43 {
				p = np42
				v.Ratio = 0
			} else {
				if x44, np45, ok46 := odjsonrt.ParseSimpleFloat(data, p, 64); ok46 {
					v.Ratio = float64(x44)
					p = np45
				} else {
					var x47 float64
					x47, p, err = odjsonrt.ParseFloat(data, p, 64)
					if err != nil {
						return p, err
					}
					v.Ratio = x47
				}
			}
		case 4:
			if strict && seen[0]&(1<<4) != 0 {
				return kp, odjsonrt.ErrDuplicateNameAt(data, kp)
			}
			seen[0] |= 1 << 4
			p, err = v.Nested.odjsonParseV2(data, p, sc, strict)
			if err != nil {
				return p, err
			}
		case 5:
			if strict && seen[0]&(1<<5) != 0 {
				return kp, odjsonrt.ErrDuplicateNameAt(data, kp)
			}
			seen[0] |= 1 << 5
			if np48, ok49 := odjsonrt.ParseNull(data, p); ok49 {
				v.Ptr = nil
				p = np48
			} else {
				if v.Ptr == nil {
					v.Ptr = new(nested)
				}
				p, err = (*v.Ptr).odjsonParseV2(data, p, sc, strict)
				if err != nil {
					return p, err
				}
			}
		default:
			if strict {
				for _, u := range unknown[umark:] {
					if string(u) == string(key) {
						return kp, odjsonrt.ErrDuplicateName(data, kp, key)
					}
				}
				unknown = odjsonrt.AddUnknownName(sc, unknown, key)
			}
			p = odjsonrt.SkipSpace(data, p)
			p, err = odjsonrt.SkipValueV2(data, p, strict)
			if err != nil {
				return p, err
			}
		}
		p = odjsonrt.SkipSpace(data, p)
		if uint(p) >= uint(len(data)) {
			return p, odjsonrt.ErrSyntax(data, p, "unexpected end of JSON input")
		}
		switch data[p] {
		case ',':
			p++
		case '}':
			odjsonrt.EndUnknownNames(sc, unknown, umark)
			return p + 1, nil
		default:
			return p, odjsonrt.ErrSyntax(data, p, "after object key:value pair")
		}
	}
}

// odjsonParseFrom decodes the next value in dec into v,
// driving the decoder token by token so the document is
// parsed once rather than twice.
func (v *Root) odjsonParseFrom(dec *jsontext.Decoder, sc *odjsonrt.StringCache) error {
	var err error
	_ = err
	if odjsonrt.WholeValue(dec) {
		var val jsontext.Value
		if val, err = dec.ReadValue(); err != nil {
			return err
		}
		_, err = v.odjsonParseV2(val, 0, sc, false)
		return err
	}
	switch odjsonrt.NextKind(dec) {
	case 'n':
		if _, err = dec.ReadToken(); err != nil {
			return err
		}
		*v = Root{}
		return nil
	case '{':
	default:
		return odjsonrt.ErrKindFrom(dec, "Root")
	}
	if _, err = dec.ReadToken(); err != nil {
		return err
	}
	for odjsonrt.NextKind(dec) != '}' {
		var key jsontext.Value
		key, err = dec.ReadValue()
		if err != nil {
			return err
		}
		idx := -1
		switch string(key) {
		case "\"name\"":
			idx = 0
		case "\"count\"":
			idx = 1
		case "\"tags\"":
			idx = 2
		case "\"ratio\"":
			idx = 3
		case "\"nested\"":
			idx = 4
		case "\"ptr\"":
			idx = 5
		}
		if idx < 0 && bytes.IndexByte(key, '\\') >= 0 {
			if name, ok := odjsonrt.UnquoteName(key); ok {
				switch string(name) {
				case "name":
					idx = 0
				case "count":
					idx = 1
				case "tags":
					idx = 2
				case "ratio":
					idx = 3
				case "nested":
					idx = 4
				case "ptr":
					idx = 5
				}
			}
		}
		switch idx {
		case 0:
			var val50 jsontext.Value
			val50, err = dec.ReadValue()
			if err != nil {
				return err
			}
			if val50[0] == 'n' {
				v.Name = ""
			} else {
				var x51 string
				x51, err = odjsonrt.ParseStringValue(val50, sc)
				if err != nil {
					return err
				}
				v.Name = x51
			}
		case 1:
			var val52 jsontext.Value
			val52, err = dec.ReadValue()
			if err != nil {
				return err
			}
			if val52[0] == 'n' {
				v.Count = 0
			} else {
				var x53 int64
				x53, _, err = odjsonrt.ParseInt(val52, 0, 64)
				if err != nil {
					return err
				}
				v.Count = int(x53)
			}
		case 2:
			var val54 jsontext.Value
			val54, err = dec.ReadValue()
			if err != nil {
				return err
			}
			vp55 := 0
			if np56, ok57 := odjsonrt.ParseNull(val54, vp55); ok57 {
				vp55 = np56
				v.Tags = nil
			} else {
				if uint(vp55) >= uint(len(val54)) || val54[vp55] != '[' {
					return odjsonrt.ErrType(val54, vp55, "[]string")
				}
				vp55++
				s58 := v.Tags[:0]
				vp55 = odjsonrt.SkipSpace(val54, vp55)
				if uint(vp55) < uint(len(val54)) && val54[vp55] == ']' {
					vp55++
				} else {
					if cap(s58) == 0 {
						s58 = make([]string, 0, odjsonrt.CapFor[string](&odjsonCapRootTags))
					}
					for {
						var e59 string
						if uint(vp55) >= uint(len(val54)) || val54[vp55] <= ' ' {
							vp55 = odjsonrt.SkipSpace(val54, odjsonrt.SkipIndent(val54, vp55))
						}
						if np60, ok61 := odjsonrt.ParseNull(val54, vp55); ok61 {
							vp55 = np60
							e59 = ""
						} else {
							var x62 string
							x62, vp55, err = odjsonrt.ParseStringWith(val54, vp55, sc)
							if err != nil {
								return err
							}
							e59 = x62
						}
						s58 = append(s58, e59)
						vp55 = odjsonrt.SkipSpace(val54, vp55)
						if uint(vp55) >= uint(len(val54)) {
							return odjsonrt.ErrSyntax(val54, vp55, "unexpected end of JSON input")
						}
						if val54[vp55] == ',' {
							vp55++
							continue
						}
						if val54[vp55] == ']' {
							vp55++
							break
						}
						return odjsonrt.ErrSyntax(val54, vp55, "after array element")
					}
					odjsonCapRootTags.Record(len(s58))
				}
				if s58 == nil {
					s58 = []string{}
				}
				v.Tags = s58
			}
			_ = vp55
		case 3:
			var val63 jsontext.Value
			val63, err = dec.ReadValue()
			if err != nil {
				return err
			}
			if val63[0] == 'n' {
				v.Ratio = 0
			} else {
				var x64 float64
				x64, err = odjsonrt.ParseFloatValue(val63, 64)
				if err != nil {
					return err
				}
				v.Ratio = x64
			}
		case 4:
			if err = v.Nested.odjsonParseFrom(dec, sc); err != nil {
				return err
			}
		case 5:
			if odjsonrt.NextKind(dec) == 'n' {
				if _, err = dec.ReadToken(); err != nil {
					return err
				}
				v.Ptr = nil
			} else {
				if v.Ptr == nil {
					v.Ptr = new(nested)
				}
				if err = (*v.Ptr).odjsonParseFrom(dec, sc); err != nil {
					return err
				}
			}
		default:
			if _, err = dec.ReadValue(); err != nil {
				return err
			}
		}
	}
	_, err = dec.ReadToken()
	return err
}

// odjsonSizeRoot sizes the buffer the encoders allocate.
var odjsonSizeRoot odjsonrt.SizeHint

// MarshalJSON implements encoding/json.Marshaler.
func (v Root) MarshalJSON() ([]byte, error) {
	buf, err := v.odjsonAppend(odjsonSizeRoot.New(), odjsonrt.ModeHTML)
	if err != nil {
		return nil, err
	}
	odjsonSizeRoot.Record(buf)
	return buf, nil
}

// UnmarshalJSON implements encoding/json.Unmarshaler.
func (v *Root) UnmarshalJSON(data []byte) error {
	// The cache plays the part of encoding/json/v2's string cache: a
	// value that recurs in the document is allocated once.
	sc := odjsonrt.GetStringCache()
	p, err := v.odjsonParse(data, odjsonrt.SkipSpace(data, 0), sc)
	odjsonrt.PutStringCache(sc)
	if err != nil {
		return err
	}
	return odjsonrt.EndOfDocument(data, p)
}

// MarshalJSONTo implements encoding/json/v2.MarshalerTo.
func (v Root) MarshalJSONTo(enc *jsontext.Encoder) error {
	// Under a plain json.Marshal, from encoding/json/v2 or encoding/json,
	// the value is appended straight into the encoder's buffer at any
	// depth; see odjsonrt.BeginDirectEncodeMode for what qualifies and
	// for the mode, which does that call's escaping itself since nothing
	// downstream looks at the bytes.
	if buf, m, ok := odjsonrt.BeginDirectEncodeMode(enc); ok {
		n := len(buf)
		buf, err := v.odjsonAppend(buf, m)
		if err != nil {
			return err
		}
		odjsonSizeRoot.Record(buf[n:])
		odjsonrt.EndDirectEncode(enc, buf)
		return nil
	}
	// Otherwise WriteValue copies what it is given, so the scratch buffer
	// can go straight back to the pool. ModeStream leaves the HTML escaping
	// and the UTF-8 validation to the encoder, which performs both while
	// reformatting the value either way.
	buf := odjsonrt.GetBuffer()
	var err error
	if buf.B, err = v.odjsonAppend(buf.B, odjsonrt.ModeStream); err == nil {
		odjsonSizeRoot.Record(buf.B)
		err = enc.WriteValue(buf.B)
	}
	odjsonrt.PutBuffer(buf)
	return err
}

// UnmarshalJSONFrom implements encoding/json/v2.UnmarshalerFrom.
func (v *Root) UnmarshalJSONFrom(dec *jsontext.Decoder) error {
	// The cache plays the part of json/v2's own string cache: a value
	// that recurs in the document is allocated once.
	sc := odjsonrt.GetStringCache()
	var err error
	// Under a plain json.Unmarshal the value is parsed straight out of
	// the decoder's buffer at any depth; see odjsonrt.BeginDirectDecodeAt
	// for what qualifies. Nobody has validated those bytes, so
	// odjsonParseV2 rejects what jsontext would have.
	if data, p, ok := odjsonrt.BeginDirectDecodeAt(dec); ok {
		var end int
		if end, err = v.odjsonParseV2(data, p, sc, true); err == nil {
			odjsonrt.EndDirectDecodeAt(dec, p, end)
		}
	} else {
		err = v.odjsonParseFrom(dec, sc)
	}
	odjsonrt.PutStringCache(sc)
	return err
}

// MarshalRoot returns the JSON encoding of v, reaching the generated
// encoder without going through encoding/json.
func MarshalRoot(v *Root) ([]byte, error) {
	// The encoding is built in a pooled buffer and copied out once,
	// rather than written into a buffer allocated at the right size:
	// a fresh allocation is handed out zeroed, so writing into it
	// walks cold memory twice, while the copy out of a warm pooled
	// buffer allocates without zeroing and moves the bytes once.
	buf := odjsonrt.GetBuffer()
	var err error
	buf.B, err = v.odjsonAppend(buf.B, odjsonrt.ModeV2HTML)
	if err != nil {
		odjsonrt.PutBuffer(buf)
		return nil, err
	}
	out := bytes.Clone(buf.B)
	odjsonrt.PutBuffer(buf)
	return out, nil
}

// AppendRoot appends the JSON encoding of v to dst and returns the
// extended buffer. On failure dst is returned with nothing appended.
func AppendRoot(dst []byte, v *Root) ([]byte, error) {
	n := len(dst)
	dst, err := v.odjsonAppend(dst, odjsonrt.ModeV2HTML)
	if err != nil {
		return dst[:n], err
	}
	return dst, nil
}

// UnmarshalRoot decodes a complete JSON document into v under
// encoding/json/v2's semantics. Nobody has validated data, so the parser
// rejects what jsontext would have.
func UnmarshalRoot(data []byte, v *Root) error {
	// The cache plays the part of encoding/json/v2's string cache: a
	// value that recurs in the document is allocated once.
	sc := odjsonrt.GetStringCache()
	p, err := v.odjsonParseV2(data, odjsonrt.SkipSpace(data, 0), sc, true)
	odjsonrt.PutStringCache(sc)
	if err != nil {
		return err
	}
	return odjsonrt.EndOfDocument(data, p)
}

// odjsonAppend appends the JSON encoding of v to dst.
func (v *nested) odjsonAppend(dst []byte, m odjsonrt.StringMode) ([]byte, error) {
	var err error
	_ = err
	start := len(dst)
	dst = append(dst, ",\"id\":"...)
	dst = odjsonrt.AppendInt(dst, int64(v.ID))
	if v.Meta == nil {
		if m.V2() {
			dst = append(dst, ",\"meta\":{}"...)
		} else {
			dst = append(dst, ",\"meta\":null"...)
		}
	} else {
		keys65 := make([]string, 0, len(v.Meta))
		for k66 := range v.Meta {
			keys65 = append(keys65, string(k66))
		}
		slices.Sort(keys65)
		dst = append(dst, ",\"meta\":{"...)
		for i67, k66 := range keys65 {
			if i67 > 0 {
				dst = append(dst, ',')
			}
			dst = append(dst, '"')
			dst, err = odjsonrt.AppendStringBodyChecked(dst, k66, m)
			if err != nil {
				return nil, err
			}
			mv68 := v.Meta[k66]
			dst = append(dst, "\":\""...)
			dst, err = odjsonrt.AppendStringBodyChecked(dst, string(mv68), m)
			if err != nil {
				return nil, err
			}
			dst = append(dst, "\""...)
		}
		dst = append(dst, "}"...)
	}
	if m.V2() || bool(v.On) {
		if bool(v.On) {
			dst = append(dst, ",\"on\":true"...)
		} else {
			dst = append(dst, ",\"on\":false"...)
		}
	}
	dst[start] = '{'
	dst = append(dst, "}"...)
	return dst, nil
}

// odjsonParse decodes the JSON object starting at p into v and
// returns the offset just past it. Strings are interned through
// sc, which may be nil.
func (v *nested) odjsonParse(data []byte, p int, sc *odjsonrt.StringCache) (int, error) {
	var err error
	_ = err
	p = odjsonrt.SkipSpace(data, p)
	if np, ok := odjsonrt.ParseNull(data, p); ok {
		return np, nil
	}
	if uint(p) >= uint(len(data)) || data[p] != '{' {
		return p, odjsonrt.ErrType(data, p, "nested")
	}
	p++
	p = odjsonrt.SkipSpace(data, p)
	if uint(p) < uint(len(data)) && data[p] == '}' {
		return p + 1, nil
	}
	for {
		var key []byte
		if uint(p) >= uint(len(data)) || data[p] <= ' ' {
			p = odjsonrt.SkipSpace(data, odjsonrt.SkipIndent(data, p))
		}
		idx := -1
		rest := data[p:]
		if len(rest) > 1 {
			switch rest[1] {
			case 'i':
				if len(rest) >= 4 && string(rest[:4]) == "\"id\"" {
					idx, p = 0, p+4
				}
			case 'm':
				if len(rest) >= 6 && string(rest[:6]) == "\"meta\"" {
					idx, p = 1, p+6
				}
			case 'o':
				if len(rest) >= 4 && string(rest[:4]) == "\"on\"" {
					idx, p = 2, p+4
				}
			}
		}
		if idx >= 0 {
			if np := odjsonrt.AfterName(data, p); np > 0 {
				p = np
			} else if p, err = odjsonrt.AfterKey(data, p); err != nil {
				return p, err
			}
		} else {
			key, _, p, err = odjsonrt.ParseKey(data, p)
			if err != nil {
				return p, err
			}
			switch string(key) {
			case "id":
				idx = 0
			case "meta":
				idx = 1
			case "on":
				idx = 2
			}
		}
		switch idx {
		case 0:
			if np69, ok70 := odjsonrt.ParseNull(data, p); ok70 {
				p = np69
			} else {
				if x71, np72, ok73 := odjsonrt.ParseDecimal(data, p); ok73 {
					v.ID = int(x71)
					p = np72
				} else {
					var x74 int64
					x74, p, err = odjsonrt.ParseInt(data, p, 64)
					if err != nil {
						return p, err
					}
					v.ID = int(x74)
				}
			}
		case 1:
			if np75, ok76 := odjsonrt.ParseNull(data, p); ok76 {
				p = np75
				v.Meta = nil
			} else {
				if uint(p) >= uint(len(data)) || data[p] != '{' {
					return p, odjsonrt.ErrType(data, p, "map[string]string")
				}
				p++
				m77 := v.Meta
				if m77 == nil {
					m77 = make(map[string]string)
				}
				p = odjsonrt.SkipSpace(data, p)
				if uint(p) < uint(len(data)) && data[p] == '}' {
					p++
				} else {
					for {
						var k78 []byte
						p = odjsonrt.SkipSpace(data, p)
						k78, _, p, err = odjsonrt.ParseKey(data, p)
						if err != nil {
							return p, err
						}
						var mv79 string
						p = odjsonrt.SkipSpace(data, p)
						if np80, ok81 := odjsonrt.ParseNull(data, p); ok81 {
							p = np80
						} else {
							var x82 string
							x82, p, err = odjsonrt.ParseStringCached(data, p, sc)
							if err != nil {
								return p, err
							}
							mv79 = x82
						}
						m77[sc.Make(k78)] = mv79
						p = odjsonrt.SkipSpace(data, p)
						if uint(p) >= uint(len(data)) {
							return p, odjsonrt.ErrSyntax(data, p, "unexpected end of JSON input")
						}
						if data[p] == ',' {
							p++
							continue
						}
						if data[p] == '}' {
							p++
							break
						}
						return p, odjsonrt.ErrSyntax(data, p, "after object key:value pair")
					}
				}
				v.Meta = m77
			}
		case 2:
			if np83, ok84 := odjsonrt.ParseNull(data, p); ok84 {
				p = np83
			} else {
				if uint(p+4) <= uint(len(data)) && string(data[p:p+4]) == "true" {
					v.On = true
					p += 4
				} else if uint(p+5) <= uint(len(data)) && string(data[p:p+5]) == "false" {
					v.On = false
					p += 5
				} else {
					var x85 bool
					x85, p, err = odjsonrt.ParseBool(data, p)
					if err != nil {
						return p, err
					}
					v.On = x85
				}
			}
		default:
			p = odjsonrt.SkipSpace(data, p)
			p, err = odjsonrt.SkipValue(data, p)
			if err != nil {
				return p, err
			}
		}
		p = odjsonrt.SkipSpace(data, p)
		if uint(p) >= uint(len(data)) {
			return p, odjsonrt.ErrSyntax(data, p, "unexpected end of JSON input")
		}
		switch data[p] {
		case ',':
			p++
		case '}':
			return p + 1, nil
		default:
			return p, odjsonrt.ErrSyntax(data, p, "after object key:value pair")
		}
	}
}

// odjsonParseV2 is odjsonParse under encoding/json/v2's semantics, for
// input a jsontext.Decoder has already validated when strict is false,
// and for bytes nobody has looked at when it is true.
func (v *nested) odjsonParseV2(data []byte, p int, sc *odjsonrt.StringCache, strict bool) (int, error) {
	var err error
	_ = err
	p = odjsonrt.SkipSpace(data, p)
	if np, ok := odjsonrt.ParseNull(data, p); ok {
		*v = nested{}
		return np, nil
	}
	if uint(p) >= uint(len(data)) || data[p] != '{' {
		return p, odjsonrt.ErrType(data, p, "nested")
	}
	p++
	p = odjsonrt.SkipSpace(data, p)
	if uint(p) < uint(len(data)) && data[p] == '}' {
		return p + 1, nil
	}
	var seen [1]uint64
	_ = seen
	unknown, umark := odjsonrt.UnknownNames(sc)
	for {
		var key []byte
		if uint(p) >= uint(len(data)) || data[p] <= ' ' {
			p = odjsonrt.SkipSpace(data, odjsonrt.SkipIndent(data, p))
		}
		kp := p
		idx := -1
		rest := data[p:]
		if len(rest) > 1 {
			switch rest[1] {
			case 'i':
				if len(rest) >= 4 && string(rest[:4]) == "\"id\"" {
					idx, p = 0, p+4
				}
			case 'm':
				if len(rest) >= 6 && string(rest[:6]) == "\"meta\"" {
					idx, p = 1, p+6
				}
			case 'o':
				if len(rest) >= 4 && string(rest[:4]) == "\"on\"" {
					idx, p = 2, p+4
				}
			}
		}
		if idx >= 0 {
			if np := odjsonrt.AfterName(data, p); np > 0 {
				p = np
			} else if p, err = odjsonrt.AfterKey(data, p); err != nil {
				return p, err
			}
		} else {
			key, p, err = odjsonrt.ParseKeyV2(data, p, strict)
			if err != nil {
				return p, err
			}
			switch string(key) {
			case "id":
				idx = 0
			case "meta":
				idx = 1
			case "on":
				idx = 2
			}
		}
		switch idx {
		case 0:
			if strict && seen[0]&(1<<0) != 0 {
				return kp, odjsonrt.ErrDuplicateNameAt(data, kp)
			}
			seen[0] |= 1 << 0
			if np86, ok87 := odjsonrt.ParseNull(data, p); ok87 {
				p = np86
				v.ID = 0
			} else {
				if x88, np89, ok90 := odjsonrt.ParseDecimal(data, p); ok90 {
					v.ID = int(x88)
					p = np89
				} else {
					var x91 int64
					x91, p, err = odjsonrt.ParseInt(data, p, 64)
					if err != nil {
						return p, err
					}
					v.ID = int(x91)
				}
			}
		case 1:
			if strict && seen[0]&(1<<1) != 0 {
				return kp, odjsonrt.ErrDuplicateNameAt(data, kp)
			}
			seen[0] |= 1 << 1
			if np92, ok93 := odjsonrt.ParseNull(data, p); ok93 {
				p = np92
				v.Meta = nil
			} else {
				if uint(p) >= uint(len(data)) || data[p] != '{' {
					return p, odjsonrt.ErrType(data, p, "map[string]string")
				}
				p++
				m94 := v.Meta
				if m94 == nil {
					m94 = make(map[string]string)
				}
				var seen97 map[string]struct{}
				if strict && len(m94) > 0 {
					seen97 = make(map[string]struct{})
				}
				p = odjsonrt.SkipSpace(data, p)
				if uint(p) < uint(len(data)) && data[p] == '}' {
					p++
				} else {
					for {
						var k95 []byte
						p = odjsonrt.SkipSpace(data, p)
						kp98 := p
						k95, p, err = odjsonrt.ParseKeyV2(data, p, strict)
						if err != nil {
							return p, err
						}
						if !strict {
						} else if seen97 == nil {
							if _, dup := m94[string(k95)]; dup {
								return p, odjsonrt.ErrDuplicateName(data, kp98, k95)
							}
						} else {
							if _, dup := seen97[string(k95)]; dup {
								return p, odjsonrt.ErrDuplicateName(data, kp98, k95)
							}
							seen97[string(k95)] = struct{}{}
						}
						var mv96 string
						p = odjsonrt.SkipSpace(data, p)
						if np99, ok100 := odjsonrt.ParseNull(data, p); ok100 {
							p = np99
							mv96 = ""
						} else {
							var x101 string
							x101, p, err = odjsonrt.ParseStringV2(data, p, sc, strict)
							if err != nil {
								return p, err
							}
							mv96 = x101
						}
						m94[sc.Make(k95)] = mv96
						p = odjsonrt.SkipSpace(data, p)
						if uint(p) >= uint(len(data)) {
							return p, odjsonrt.ErrSyntax(data, p, "unexpected end of JSON input")
						}
						if data[p] == ',' {
							p++
							continue
						}
						if data[p] == '}' {
							p++
							break
						}
						return p, odjsonrt.ErrSyntax(data, p, "after object key:value pair")
					}
				}
				v.Meta = m94
			}
		case 2:
			if strict && seen[0]&(1<<2) != 0 {
				return kp, odjsonrt.ErrDuplicateNameAt(data, kp)
			}
			seen[0] |= 1 << 2
			if np102, ok103 := odjsonrt.ParseNull(data, p); ok103 {
				p = np102
				v.On = false
			} else {
				if uint(p+4) <= uint(len(data)) && string(data[p:p+4]) == "true" {
					v.On = true
					p += 4
				} else if uint(p+5) <= uint(len(data)) && string(data[p:p+5]) == "false" {
					v.On = false
					p += 5
				} else {
					var x104 bool
					x104, p, err = odjsonrt.ParseBool(data, p)
					if err != nil {
						return p, err
					}
					v.On = x104
				}
			}
		default:
			if strict {
				for _, u := range unknown[umark:] {
					if string(u) == string(key) {
						return kp, odjsonrt.ErrDuplicateName(data, kp, key)
					}
				}
				unknown = odjsonrt.AddUnknownName(sc, unknown, key)
			}
			p = odjsonrt.SkipSpace(data, p)
			p, err = odjsonrt.SkipValueV2(data, p, strict)
			if err != nil {
				return p, err
			}
		}
		p = odjsonrt.SkipSpace(data, p)
		if uint(p) >= uint(len(data)) {
			return p, odjsonrt.ErrSyntax(data, p, "unexpected end of JSON input")
		}
		switch data[p] {
		case ',':
			p++
		case '}':
			odjsonrt.EndUnknownNames(sc, unknown, umark)
			return p + 1, nil
		default:
			return p, odjsonrt.ErrSyntax(data, p, "after object key:value pair")
		}
	}
}

// odjsonParseFrom decodes the next value in dec into v,
// driving the decoder token by token so the document is
// parsed once rather than twice.
func (v *nested) odjsonParseFrom(dec *jsontext.Decoder, sc *odjsonrt.StringCache) error {
	var err error
	_ = err
	if odjsonrt.WholeValue(dec) {
		var val jsontext.Value
		if val, err = dec.ReadValue(); err != nil {
			return err
		}
		_, err = v.odjsonParseV2(val, 0, sc, false)
		return err
	}
	switch odjsonrt.NextKind(dec) {
	case 'n':
		if _, err = dec.ReadToken(); err != nil {
			return err
		}
		*v = nested{}
		return nil
	case '{':
	default:
		return odjsonrt.ErrKindFrom(dec, "nested")
	}
	if _, err = dec.ReadToken(); err != nil {
		return err
	}
	for odjsonrt.NextKind(dec) != '}' {
		var key jsontext.Value
		key, err = dec.ReadValue()
		if err != nil {
			return err
		}
		idx := -1
		switch string(key) {
		case "\"id\"":
			idx = 0
		case "\"meta\"":
			idx = 1
		case "\"on\"":
			idx = 2
		}
		if idx < 0 && bytes.IndexByte(key, '\\') >= 0 {
			if name, ok := odjsonrt.UnquoteName(key); ok {
				switch string(name) {
				case "id":
					idx = 0
				case "meta":
					idx = 1
				case "on":
					idx = 2
				}
			}
		}
		switch idx {
		case 0:
			var val105 jsontext.Value
			val105, err = dec.ReadValue()
			if err != nil {
				return err
			}
			if val105[0] == 'n' {
				v.ID = 0
			} else {
				var x106 int64
				x106, _, err = odjsonrt.ParseInt(val105, 0, 64)
				if err != nil {
					return err
				}
				v.ID = int(x106)
			}
		case 1:
			var val107 jsontext.Value
			val107, err = dec.ReadValue()
			if err != nil {
				return err
			}
			vp108 := 0
			if np109, ok110 := odjsonrt.ParseNull(val107, vp108); ok110 {
				vp108 = np109
				v.Meta = nil
			} else {
				if uint(vp108) >= uint(len(val107)) || val107[vp108] != '{' {
					return odjsonrt.ErrType(val107, vp108, "map[string]string")
				}
				vp108++
				m111 := v.Meta
				if m111 == nil {
					m111 = make(map[string]string)
				}
				vp108 = odjsonrt.SkipSpace(val107, vp108)
				if uint(vp108) < uint(len(val107)) && val107[vp108] == '}' {
					vp108++
				} else {
					for {
						var k112 []byte
						vp108 = odjsonrt.SkipSpace(val107, vp108)
						k112, _, vp108, err = odjsonrt.ParseKey(val107, vp108)
						if err != nil {
							return err
						}
						var mv113 string
						vp108 = odjsonrt.SkipSpace(val107, vp108)
						if np114, ok115 := odjsonrt.ParseNull(val107, vp108); ok115 {
							vp108 = np114
							mv113 = ""
						} else {
							var x116 string
							x116, vp108, err = odjsonrt.ParseStringWith(val107, vp108, sc)
							if err != nil {
								return err
							}
							mv113 = x116
						}
						m111[sc.Make(k112)] = mv113
						vp108 = odjsonrt.SkipSpace(val107, vp108)
						if uint(vp108) >= uint(len(val107)) {
							return odjsonrt.ErrSyntax(val107, vp108, "unexpected end of JSON input")
						}
						if val107[vp108] == ',' {
							vp108++
							continue
						}
						if val107[vp108] == '}' {
							vp108++
							break
						}
						return odjsonrt.ErrSyntax(val107, vp108, "after object key:value pair")
					}
				}
				v.Meta = m111
			}
			_ = vp108
		case 2:
			var val117 jsontext.Value
			val117, err = dec.ReadValue()
			if err != nil {
				return err
			}
			if val117[0] == 'n' {
				v.On = false
			} else {
				var x118 bool
				x118, _, err = odjsonrt.ParseBool(val117, 0)
				if err != nil {
					return err
				}
				v.On = x118
			}
		default:
			if _, err = dec.ReadValue(); err != nil {
				return err
			}
		}
	}
	_, err = dec.ReadToken()
	return err
}

// odjsonSizenested sizes the buffer the encoders allocate.
var odjsonSizenested odjsonrt.SizeHint

// MarshalJSON implements encoding/json.Marshaler.
func (v nested) MarshalJSON() ([]byte, error) {
	buf, err := v.odjsonAppend(odjsonSizenested.New(), odjsonrt.ModeHTML)
	if err != nil {
		return nil, err
	}
	odjsonSizenested.Record(buf)
	return buf, nil
}

// UnmarshalJSON implements encoding/json.Unmarshaler.
func (v *nested) UnmarshalJSON(data []byte) error {
	// The cache plays the part of encoding/json/v2's string cache: a
	// value that recurs in the document is allocated once.
	sc := odjsonrt.GetStringCache()
	p, err := v.odjsonParse(data, odjsonrt.SkipSpace(data, 0), sc)
	odjsonrt.PutStringCache(sc)
	if err != nil {
		return err
	}
	return odjsonrt.EndOfDocument(data, p)
}

// MarshalJSONTo implements encoding/json/v2.MarshalerTo.
func (v nested) MarshalJSONTo(enc *jsontext.Encoder) error {
	// Under a plain json.Marshal, from encoding/json/v2 or encoding/json,
	// the value is appended straight into the encoder's buffer at any
	// depth; see odjsonrt.BeginDirectEncodeMode for what qualifies and
	// for the mode, which does that call's escaping itself since nothing
	// downstream looks at the bytes.
	if buf, m, ok := odjsonrt.BeginDirectEncodeMode(enc); ok {
		n := len(buf)
		buf, err := v.odjsonAppend(buf, m)
		if err != nil {
			return err
		}
		odjsonSizenested.Record(buf[n:])
		odjsonrt.EndDirectEncode(enc, buf)
		return nil
	}
	// Otherwise WriteValue copies what it is given, so the scratch buffer
	// can go straight back to the pool. ModeStream leaves the HTML escaping
	// and the UTF-8 validation to the encoder, which performs both while
	// reformatting the value either way.
	buf := odjsonrt.GetBuffer()
	var err error
	if buf.B, err = v.odjsonAppend(buf.B, odjsonrt.ModeStream); err == nil {
		odjsonSizenested.Record(buf.B)
		err = enc.WriteValue(buf.B)
	}
	odjsonrt.PutBuffer(buf)
	return err
}

// UnmarshalJSONFrom implements encoding/json/v2.UnmarshalerFrom.
func (v *nested) UnmarshalJSONFrom(dec *jsontext.Decoder) error {
	// The cache plays the part of json/v2's own string cache: a value
	// that recurs in the document is allocated once.
	sc := odjsonrt.GetStringCache()
	var err error
	// Under a plain json.Unmarshal the value is parsed straight out of
	// the decoder's buffer at any depth; see odjsonrt.BeginDirectDecodeAt
	// for what qualifies. Nobody has validated those bytes, so
	// odjsonParseV2 rejects what jsontext would have.
	if data, p, ok := odjsonrt.BeginDirectDecodeAt(dec); ok {
		var end int
		if end, err = v.odjsonParseV2(data, p, sc, true); err == nil {
			odjsonrt.EndDirectDecodeAt(dec, p, end)
		}
	} else {
		err = v.odjsonParseFrom(dec, sc)
	}
	odjsonrt.PutStringCache(sc)
	return err
}

// marshalNested returns the JSON encoding of v, reaching the generated
// encoder without going through encoding/json.
func marshalNested(v *nested) ([]byte, error) {
	// The encoding is built in a pooled buffer and copied out once,
	// rather than written into a buffer allocated at the right size:
	// a fresh allocation is handed out zeroed, so writing into it
	// walks cold memory twice, while the copy out of a warm pooled
	// buffer allocates without zeroing and moves the bytes once.
	buf := odjsonrt.GetBuffer()
	var err error
	buf.B, err = v.odjsonAppend(buf.B, odjsonrt.ModeV2HTML)
	if err != nil {
		odjsonrt.PutBuffer(buf)
		return nil, err
	}
	out := bytes.Clone(buf.B)
	odjsonrt.PutBuffer(buf)
	return out, nil
}

// appendNested appends the JSON encoding of v to dst and returns the
// extended buffer. On failure dst is returned with nothing appended.
func appendNested(dst []byte, v *nested) ([]byte, error) {
	n := len(dst)
	dst, err := v.odjsonAppend(dst, odjsonrt.ModeV2HTML)
	if err != nil {
		return dst[:n], err
	}
	return dst, nil
}

// unmarshalNested decodes a complete JSON document into v under
// encoding/json/v2's semantics. Nobody has validated data, so the parser
// rejects what jsontext would have.
func unmarshalNested(data []byte, v *nested) error {
	// The cache plays the part of encoding/json/v2's string cache: a
	// value that recurs in the document is allocated once.
	sc := odjsonrt.GetStringCache()
	p, err := v.odjsonParseV2(data, odjsonrt.SkipSpace(data, 0), sc, true)
	odjsonrt.PutStringCache(sc)
	if err != nil {
		return err
	}
	return odjsonrt.EndOfDocument(data, p)
}
